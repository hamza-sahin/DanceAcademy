CREATE TABLE `Users` (
  `User_ID` int NOT NULL AUTO_INCREMENT,
  `Name` varchar(50),
  `Surname` varchar(50),
  `E-mail` varchar(50),
  `Password` varchar(50),
  PRIMARY KEY (`User_ID`),
  KEY `Key` (`Name`, `Surname`, `E-mail`, `Password`)
);

CREATE TABLE `Instructors` (
  `Instructor_ID` int NOT NULL AUTO_INCREMENT,
  `User_ID` int,
  `Description` longtext,
  `num_of_students` int,
  PRIMARY KEY (`Instructor_ID`),
  FOREIGN KEY (`User_ID`) REFERENCES Users(User_ID),
  KEY `Key` (`num_of_students`)
);


CREATE TABLE `Courses` (
  `Course_ID` int NOT NULL AUTO_INCREMENT,
  `Instructor_ID` int,
  `Title` varchar(50),
  `Genre` varchar(50),
  `Price` double,
  `Rating` float,
  `num_of_enrolments` int,
  `Description` longtext,
  `Publish_date` datetime,
  PRIMARY KEY (`Course_ID`),
  FOREIGN Key (`Instructor_ID`) REFERENCES Instructors(Instructor_ID),
  KEY `Key` (`Title`, `Genre`, `Price`, `Rating`, `num_of_enrolments`, `Publish_date`)
);

CREATE TABLE `Syllabuses` (
  `Syllabus_ID` int NOT NULL AUTO_INCREMENT,
  `Course_ID` int,
  `Syllabus_Name` varchar(50),
  PRIMARY KEY (`Syllabus_ID`),
  FOREIGN KEY (`Course_ID`) REFERENCES Courses(Course_ID),
  KEY `Key` (`Syllabus_Name`)
);

CREATE TABLE `Orders` (
  `Order_ID` int NOT NULL AUTO_INCREMENT,
  `Course_ID` int,
  `User_ID` int,
  `Order_Date` datetime,
  PRIMARY KEY (`Order_ID`),
  FOREIGN Key (`Course_ID`) REFERENCES Courses(Course_ID),
    FOREIGN Key (`User_ID`) REFERENCES Users(User_ID),
  KEY `Key` (`Order_Date`)
);

CREATE TABLE `Contents` (
  `Content_ID` int NOT NULL AUTO_INCREMENT,
  `Syllabus_ID` int,
  `Title` varchar(50),
  `Description` longtext,
  `Video` BLOB,
  PRIMARY KEY (`Content_ID`),
  FOREIGN KEY (`Syllabus_ID`) REFERENCES Syllabuses(Syllabus_ID),
  KEY `Key` (`Title`)
);

